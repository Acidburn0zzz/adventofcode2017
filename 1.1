#!/usr/bin/lua

local input = io.read("*all"):sub(1,-2)
local total = 0

for i = 1,#input do
	local inext = (i % #input) + 1
	local c = input:sub(i,i)
	local cnext = input:sub(inext, inext)

	if c == cnext then
		total = total + c
	end
end

print(total)
